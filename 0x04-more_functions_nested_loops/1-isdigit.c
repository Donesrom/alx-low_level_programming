#include "main.h"
  

/**
 * int _isdigit -  function that checks for a digit (0 through 9)
 *@n int to check
 * Return: 1 if c is a digit, 0 otherwise
 */

int _isdigit(int c)

{
return (c >= '0' && c <= '9');  
}
